import React from "react";
import { Divider as MUIDivider } from "@mui/material";

const Divider = () => {
  return <MUIDivider sx={{ borderColor: "#bababa", marginY: 2 }} />;
};

export default Divider;
